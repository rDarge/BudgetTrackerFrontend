/** @type {import('next').NextConfig} */
const nextConfig = {

    // headers: () => [
    //     {
    //         source: "/",
    //         headers: [{ key: "Access-Control-Allow-Origin", value: "*" }]
    //     }
    // ]
};

export default nextConfig;
